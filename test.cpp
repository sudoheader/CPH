#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    // int a, b;
    // string x;
    // cin >> a >> b >> x;
    int a = 123, b = 456;
    string x = "monkey";
    cout << a << " " << b << " " << x << "\n"; // endl always causes flush operation so "\n" is faster
}
